import 'package:flutter/material.dart';

class FilaHome extends StatelessWidget {
  const FilaHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Container()],
      ),
    );
  }
}
